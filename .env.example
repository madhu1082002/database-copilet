# Primary key (optional — uses first key from GEMINI_API_KEYS if not set)
GOOGLE_API_KEY=

# Comma-separated keys for automatic fallback (Key 1 → Key 2 → ...)
GEMINI_API_KEYS=key1,key2,key3

USE_MOCK_AI=false
